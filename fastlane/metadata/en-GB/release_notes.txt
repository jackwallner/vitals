The Vitals+ screen you see during setup now shows a worked example of the numbers it builds, instead of a list of feature names.

Where Apple Health already holds enough history, that screen works from your own data rather than a stand-in figure. Where it doesn't, it says so instead of inventing one.
