A tela do Vitals+ que aparece durante a configuração agora mostra um exemplo resolvido dos números que ele produz, em vez de uma lista de nomes de recursos.

Quando o Apple Health já tem histórico suficiente, essa tela trabalha com os seus próprios dados em vez de um valor de exemplo. Quando não tem, ela diz isso em vez de inventar um.
